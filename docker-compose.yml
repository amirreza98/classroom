services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - classroom-network

  kafka:
    image: apache/kafka:latest
    container_name: kafka
    restart: unless-stopped
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs
      CLUSTER_ID: 5L6g3nShT-eMCtK--X86sw
    networks:
      - classroom-network

  gateway-service:
    build: ./gateway-service
    container_name: gateway-service
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      EXPRESS_SERVICE_URL: http://auth-service:8000
      ACADEMIC_SERVICE_URL: http://academic-service:8001
      VOICE_SERVICE_URL: http://voice-service:3001
      NOTIFICATION_SERVICE_URL: http://notifications-service:8082
      COLLABORATION_SERVICE_URL: http://collaboration-service:8083
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWK_SET_URI: http://auth-service:8000/api/auth/jwks
    depends_on:
      - redis
      - kafka
      - auth-service
    networks:
      - classroom-network

  auth-service:
    build: ./auth-service
    container_name: auth-service
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./auth-service/.env
    environment:
      KAFKA_BROKER: kafka:9092
    depends_on:
      - kafka
      - redis
    networks:
      - classroom-network

  academic-service:
    build: ./academic-service
    container_name: academic-service
    restart: unless-stopped
    ports:
      - "8001:8001"
    environment:
      SERVER_PORT: 8001
      KAFKA_BROKER: kafka:9092
      AUTH_SERVICE_URL: http://auth-service:8000
      DATABASE_URL: ${ACADEMIC_DATABASE_URL}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      FRONTEND_URL: ${FRONTEND_URL}
    depends_on:
      - kafka
      - redis
      - auth-service
    networks:
      - classroom-network

  notifications-service:
    build: ./notifications-service
    container_name: notifications-service
    restart: unless-stopped
    ports:
      - "8082:8082"
    environment:
      SERVER_PORT: 8082
      KAFKA_BROKER: kafka:9092
      DATABASE_URL: ${NOTIFICATIONS_DATABASE_URL}
    depends_on:
      - kafka
    networks:
      - classroom-network

  voice-service:
    build: ./voice-service
    container_name: voice-service
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - ./voice-service/.env
    environment:
      KAFKA_BROKER: kafka:9092
    depends_on:
      - kafka
    networks:
      - classroom-network

  collaboration-service:
    build: ./collaboration-service
    container_name: collaboration-service
    restart: unless-stopped
    ports:
      - "8083:8083"
    environment:
      SERVER_PORT: 8083
      REDIS_HOST: redis
      REDIS_PORT: 6379
      DATABASE_URL: ${COLLABORATION_DATABASE_URL}
    depends_on:
      - redis
    networks:
      - classroom-network

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
    networks:
      - classroom-network

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-classroom123}
      GF_USERS_ALLOW_SIGN_UP: false
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
    depends_on:
      - prometheus
    networks:
      - classroom-network

networks:
  classroom-network:
    driver: bridge

volumes:
  prometheus_data:
  grafana_data: